# EMS Agent 自我改进记录

此文件由系统自动管理。当用户通过 Web UI 接受 AI 改进建议时，内容将自动追加到此处。
Agent 在每次告警分析时会读取此文件并将其内容注入到系统提示词中，帮助持续提升分析质量。

---

## 2026-03-31T10:09:59.454+08:00 (alarmId: TEST-1774922831050)

- 针对强硬件属性的告警（如“BMS单体电压低”）被标记为“software”的情况，提示词应增加**分类置信度校验指令**，要求 Agent 主动核查数据特征（如电压曲线是否连续跳变），避免被错误标签误导推理方向。
- 针对“BMS单体电压低”场景，预置**关键数据强制检查清单**（必须包含最小单体电压值、电压压差、充放电电流、电芯温度），确保推理能覆盖采样异常与真实电芯故障的区分。
- 优化工具调用策略，将**单体电压波形查询**作为电压类告警的第一优先级工具，优于通用日志检索，以减少无效查询次数（本次迭代3次偏多）并快速锁定异常单体。
- 知识库中需补充**BMS典型故障模式特征**：例如电芯过放表现为缓慢下降，而采样故障表现为瞬间跌落或跳变，以辅助 Agent 提升根因定位的准确性。
- 强化 P0 级告警的结论生成要求，即使无法得出确定性根因，也必须输出**基于当前数据的紧急处置建议**（如“建议立即停机检查”），禁止输出空泛或无建议的结论。


---

