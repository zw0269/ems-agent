# EMS Agent 自我改进积累

> 最近一次聚合：2026-05-01T07:56:08.073+08:00，共 19 条建议（原始 19 条，去重后 19 条）

---

## 2026-03-31T10:58:01.982+08:00 (alarmId: TEST-1774925781037)

- **补充典型阈值参考范围**：在提示词或知识库中增加PCS设备的行业典型阈值范围（如交流相电压过压保护通常在260V-270V，242V左右通常为预警上限）。这能帮助AI在缺少现场配置数据时，快速判断243V是否处于“真过高”区间，从而更精准地指向“阈值设置过低”或“误报”的根因假设。
- **预置“运行态告警”典型故障模式**：增加一条典型故障逻辑规则——当检测到“设备处于运行中（OperatingStatus正常）+ 总故障状态为False + 仅有单点告警”时，优先将搜索路径收敛于“定值/阈值配置不合理”、“采样回路偏差”或“信号误发”，减少对真实硬件故障的无效发散。
- **优化关键信号缺失的应对策略**：针对证据链中“未观测到过压保护动作状态位（点表未提供）”的情况，应在首轮推理中显式声明此类关键数据的缺失，并直接推导出“需现场人工确认保护动作逻辑”的结论，避免因反复寻找该数据导致推理迭代次数过多（本次8次）。
- **强化逻辑冲突时的根因收敛**：针对“P0高危告警”与“系统/设备运行正常”的逻辑冲突，应强制提示AI优先排查“软件/配置侧”原因（如控制字、死区设置），在结论中明确将“配置校验”作为第一优先级操作，而非与硬件检查并列。
- **细化操作建议的执行顺序**：在给出的操作建议中，应严格区分“不停机远程核查（定值、趋势）”与“需停机电测（采样回路、绝缘）”，优先推荐通过EMS或本地监控核对配置参数，以降低现场运维的试错成本和安全风险。

---

## 2026-04-01T07:35:06.438+08:00 (alarmId: 8351)

- **补全交叉验证逻辑：** 在得出“阈值不匹配”结论前，强制对比 BMS 上报总压与 PCS 采样电压，并检索具体的阈值设定值（如 `pcs_dc_ov_threshold`），以区分“真实过压（BMS 充电失控）”与“阈值误报”，避免因缺乏配置数据导致的误判。
- **预置典型阈值知识库：** 为常见 PCS 型号预置标准过压保护阈值范围（如 1000V 系统通常在 950~1000V 触发），以便在分析时快速判断 946.7V 是否处于临界触发状态，辅助验证阈值设置的合理性。
- **细化电压数据的差异分析：** 明确指出 `pcsInputVoltage` (869.4 V) 与 `pcsTotalDirectVol` (946.7 V) 之间的显著偏差，需在推理中排查是否存在采样回路故障、相间不平衡或点位含义理解错误，而非仅引用单一数值。
- **优化根因结论的严谨性：** 在未直接获取设备配置参数的情况下，应将“阈值设置不匹配”定性为“高概率疑似原因”，同时保留“BMS 充电异常导致真实过压”作为备选根因，避免结论过于绝对。
- **提升工具调用效率：** 针对软件保护类告警，优化工具调用序列，将“获取设备保护定值/参数配置”的操作前置，以减少 4 次迭代中的无效推理，更快锁定参数异常类问题。

---

## 2026-04-01T07:35:26.129+08:00 (alarmId: 8352)

- **增强内部数据一致性校验逻辑**：提示词中应增加指令，强制 Agent 在对比外部输入与内部母线电压前，先校验母线电压的内部一致性（即判断 `UpVol + DownVol` 是否约等于 `TotalDirectVol`）。本次分析中两者差值达 114V（956.4V vs 842.2V），逻辑上应优先锁定为采样计算或数据传输错误，而非物理电压不匹配，以提升根因定位精度。
- **预置“电压不匹配”故障排查决策树**：在领域知识库中预置典型排查路径，引导 Agent 优先区分三类模式：1. 采样故障（数据自相矛盾）；2. 参数标定错误（数据稳定但存在固定偏差）；3. 硬件预充电失败（电压真实无法建立）。本次分析未能利用数据内部的算术逻辑矛盾快速排除硬件故障假设。
- **优化数据采样与时序对齐策略**：针对 6 次迭代次数较多的问题，建议在工具调用层面强制要求获取同一时间戳的“数据快照”，或对 `getPcsYx` 和 `getPcsYc` 的结果进行时间戳对齐检查，避免因调用异步导致的数值跳变（如初始 956.5V 到后续 842.2V）引发无效推理循环。
- **提升操作建议对数据异常的针对性**：当检测到遥测数据内部算术不符（如分压和 ≠ 总压）时，操作建议应直接指向“检查采样系数配置、通讯解析或寄存器映射”，而非通用的“万用表实测”，减少现场人员的无效排查工作量。
- **引入 PCS 工况上下文判断**：提示词应补充对 PCS 运行状态机（待机、预充电、并机）的关联分析。不同阶段下“电压不匹配”的容忍机制不同，结合状态机可剔除状态切换过程中的瞬时误报数据，进一步收敛结论范围。

---

## 2026-04-07T11:53:52.986+08:00 (alarmId: 8372)

- 增强数据一致性校验逻辑：Agent 发现设备处于“备用”状态但存在较大电池电流（-116.2A）时，将其误判为“PCS侧无负载”。需改进提示词，要求强制比对“设备状态码”与“功率/电流”数据的物理匹配性，避免将异常放电或非预期电流工况解读为正常待机。
- 补充“VF 离网状态”的典型故障模式知识：当前分析未深究“VF 离网”的触发源。建议预置 VF（电压/频率）保护逻辑，当出现此类告警时，优先关联分析电网侧电压/频率突变数据，而非局限于 PCS 内部软件故障字，以识别是外部电网扰动导致。
- 优化“瞬时故障恢复”类告警的推理路径：本次处理迭代 8 次，说明在确认故障已恢复的过程中存在冗余查询。应改进工具调用策略，若首次查询“当前活跃故障”为空，直接调用“历史告警详情”确认恢复时序，避免多次反复查询实时状态。
- 完善设备状态机知识库：分析中未解释为何“备用”状态下会有 100kW 级别的电池功率流动。需补全 PCS 在待机、预充电、重启等过渡状态下的特征定义，防止将状态机卡死或中间过渡态误认为是正常“自动恢复”。
- 提升操作建议的安全性：在电流异常（-116.2A）且状态不明的情况下，直接建议“手动重启”可能存在风险。需通过提示词约束，在未排除负载侧或回路异常前，优先建议“检查回路负载/闭锁复位”，避免给出可能导致设备冲击的操作指令。

---

## 2026-04-15T13:21:18.559+08:00 (alarmId: TEST-1776230378319)

- **引入 SOE（事件顺序记录）强制验证**：针对“历史告警记录为空”但收到告警的场景，推理逻辑存在漏洞（直接推断电网扰动）。改进建议：必须在分析链中增加 SOE 或故障录波数据的检索，若无电气波形证据支撑，应优先考虑通讯抖动或数据采集丢失，而非直接定性为电网故障。
- **补充保护定值的显式比对**：结论建议核对定值，但证据链中未展示定值数据。改进建议：在工具调用中必须包含读取 PCS 保护参数（电压/频率 thresholds、延时 time_delay）的步骤，将告警时刻的实测波动值与定值进行量化比对，以增强结论的说服力。
- **预置“瞬时恢复”类告警的标准化分析路径**：迭代次数（5次）较多反映流程冗余。改进建议：预置针对瞬时故障的分析模板——“校准时间窗口 -> 检查 SOE -> 确认当前复归条件 -> 对比定值”，通过固定步骤减少反复尝试。
- **增加“数据完整性”故障模式分支**：领域知识中缺乏对“空记录”的深度研判。改进建议：在知识库中加入“告警存在但日志缺失”的典型故障模式，涵盖时钟同步错误、日志覆盖、采集器缓存溢出等原因，防止误将系统问题判别为设备故障。
- **提升操作建议的审慎度与风险控制**：直接建议“放宽阈值”存在运行风险。改进建议：调整结论输出的规则约束，对于单次且缺乏录波证据的告警，首选建议应为“加装电能质量监测装置”或“排查通讯链路”，仅在确认为频发性越限时才提议调整定值。

---

## 2026-04-16T12:23:32.482+08:00 (alarmId: 8624)

- 修正设备状态判定逻辑，准确区分“故障清除”与“设备恢复运行”。证据显示当前 `pcsOperatingStatus=0`（停机），结论中称“系统已自动恢复”存在逻辑矛盾，应明确指出虽故障告警已清除，但设备仍处于停机待启动状态，避免误导运维。
- 完善时序数据分析逻辑，避免用恢复后的稳态数据解释瞬态根因。243.2V 低于典型过压保护阈值（260-270V），不能直接作为触发过压离网的证据，需修正推理链，指出缺失“故障时刻电压瞬时峰值”这一关键数据，防止因数据采样率低导致的错误归因。
- 优先调用故障录波或详细事件日志工具以减少迭代次数。本次分析迭代 6 次，效率较低，后续处理 P1 级总故障时，应调整工具调用顺序，优先获取高分辨率的故障录波或 SOE 事件记录，以捕获触发瞬间的真实电气量。
- 补充预置的 PCS 标准保护定值知识库。针对 VF 保护等常见故障，应在提示词中预置该设备型号的标准电压/频率阈值（如 1.1 倍额定电压），使 Agent 能直接比对遥测数据是否超标，减少输出“建议核查阈值配置”等模糊性建议。
- 校准相间不平衡的判定阈值。C 相（243.2V）与 A/B 相（约 241V）偏差不足 1%，属于正常允许范围，不应将其定性为“相间不平衡”故障，需在领域知识中设定具体的百分比偏差阈值（如 >2%），以避免制造虚假疑点。

---

## 2026-04-19T06:02:46.432+08:00 (alarmId: 8635)

- **预置“状态矛盾”典型故障模式**：建立 `pcsOverallFaultStatus`（总故障）与各子系统故障等级（如 sort=1）的强一致性校验规则。当总状态正常但存在 P1 故障信号时，优先识别为配置/逻辑错误而非真实电网故障，减少对电网数据的过度分析。
- **补充 PCS 运行模式判据**：增加对 VF（电压频率）控制模式的细分逻辑。明确在 VF 模式下，当 `pcsOutputActivePowerTotal` 有输出且 `pcsOperatingStatus` 正常时，`pcsGridTiedStatus=false` 属于状态位定义或逻辑闭环问题，不应直接判定为物理离网故障。
- **引入“告警抖动/闪烁”特征识别**：预置短时多次触发（如30秒内 >2次）且自动恢复的特征算法。该模式应直接关联到采样回路干扰或保护定值裕度不足，避免将其误分析为真实电网扰动。
- **优化结论的严谨性**：区分“已验证事实”与“合理假设”。建议将“定值过于敏感”的结论调整为“疑似保护逻辑误动”，并列出“定值核查”作为下一步验证动作，避免在未获取具体定值数据前下绝对定论。
- **增强时序数据关联分析**：在分析电网频率波动时，应要求工具单次调取故障前后（如 -60s 至 +60s）的完整趋势曲线，而非仅对比告警时刻与当前时刻的数值，以防遗漏导致误触发的瞬时尖峰。

---

## 2026-04-19T06:03:14.430+08:00 (alarmId: 8643)

- **预置 VF 保护定值标准知识**：在系统提示词中注入并网标准（如 GB/T 19964）中关于频率/电压保护的典型阈值范围及死区逻辑，使 Agent 能直接判断 49.66Hz 是否处于临界敏感区，减少对“核对阈值”等模糊建议的依赖。
- **强化“振荡/乒乓切换”模式识别**：针对 8635/8637 等短时间内多次触发的离网告警，预置“告警振荡”典型故障模式，一旦检测到“多次触发与恢复”特征，应优先推理定值死区过小或采样干扰，优先级高于单一故障排查。
- **强制多源数据交叉校验逻辑**：对于 VF 离网类告警，固定推理步骤必须包含“PCS 内部采样”与“外部电表/电网采集”的频差/压差对比，以快速界定是真实电网扰动还是 PCS 侧采样误差导致的误触发。
- **优化工具调用策略减少迭代**：建议将历史告警查询、实时遥测获取及设备状态检查整合为一次“设备全景上下文”调用，避免因分步获取不同维度数据导致本次分析耗时 6 次迭代。
- **结论需指向具体配置参数**：在操作建议中，应直接指令读取对应的保护逻辑寄存器值（如频率保护下限 `F_low_set`），而非泛泛地建议“查看保护定值”，以确保结论能直接指导现场修改具体参数。

---

## 2026-04-19T06:04:17.691+08:00 (alarmId: 8643)

- **细化采样扰动验证逻辑**：在怀疑“电网采样扰动”作为根因时，推理过程应增加对电压谐波总畸变率（THD）、电压暂降/暂升及 dV/dt 波动数据的检索与验证，而非仅依赖电压/频率有效值处于国标区间这一单一证据，以确保“扰动”结论有据可依。
- **增强定值比对能力**：提示词应指令 Agent 在分析此类告警时，必须检索并展示 PCS 内部的 VF 保护定值（如频率/电压上限、回差、延时），将实时采样值与配置阈值进行显性数值比对，避免仅凭波形现象推断“配置偏严”。
- **预设“虚假保护”典型故障树**：针对“外部电网正常但设备触发离网保护”的典型场景，预置固定的推理路径（状态核查 -> 电网质量 -> 定值阈值比对 -> 采样回路排查），以减少无效推理次数（本次6次偏多），提升分析效率。
- **完善物理层排查建议**：在操作建议中补充具体的物理排查项，如检查 PT/CT 接线端子松动、信号线屏蔽层接地状态及采样板零点漂移校准，以提升针对“采样异常”问题的现场落地指导性。
- **明确VF离网触发源分类**：在领域知识库中补充区分 VF 离网是由“硬件保护电路动作”还是“软件逻辑检测”触发的判断依据，以便更精准地定位问题归属（控制逻辑 vs 硬件采样），避免归因过于笼统。

---

## 2026-04-19T06:04:59.559+08:00 (alarmId: 8647)

- **预置典型故障模式，优化推理路径**：针对“VF离网”场景，应在系统中固化“优先检测外部电网电气量（电压/频率）”的专家规则。本次分析迭代了6次，说明可能陷入了先排查PCS内部软件故障的误区，需引导Agent一旦发现三相电压异常，立即中止对设备软件层面的排查。
- **增强三相不平衡度量化计算**：提示词应要求输出基于 A/B/C 三相电压计算的“电压不平衡度”（负序分量百分比），而非仅罗列各相电压值。这能将差异“26.6V”转化为符合电力行业标准的判定指标，增强证据链的专业性。
- **修正“软件”分类标签的误导性**：本次告警分类为“software”，但根因为外部电网。需在提示词中明确：“VF 离网类告警即便标记为软件，通常也是保护逻辑动作，而非程序Bug”，防止 Agent 浪费时间去分析代码逻辑。
- **整合工具调用以减少迭代次数**：分析过程共6次迭代，效率偏低。建议将 PCS 关键状态量（三相电压、并网状态、VF标志、故障字）设计为一个批量查询接口，避免分多次调用获取碎片化数据，提升第一轮分析的数据完整度。
- **细化定值核对逻辑**：操作建议中“检查定值”较空泛。应要求 Agent 在分析阶段直接调取 VF 保护的设定阈值（如欠压保护点 185V 等），与实测的 183.5V 进行直接比对，以确认是电网越限还是定值敏感，从而给出更精确的调整建议。

---

## 2026-04-19T06:05:29.340+08:00 (alarmId: 8651)

- **预置典型故障模式知识**：针对“VF离网/孤岛保护”等保护类告警，预置“参数正常但状态异常”的反向推理规则；当检测到电压/频率在正常阈值内且无硬件故障字时，直接触发“信号采样/IO回路故障”分析分支，以减少反复确认电网状态带来的无效迭代。
- **引入“功率-状态”一致性校验逻辑**：在推理过程中增加强逻辑约束，若监测到“有功功率>阈值”（如16.9kW）但“并网状态=false”，应优先推断为“状态采样回路失效”或“反馈信号逻辑错误”，而非解释为真实的电网离网，以快速锁定信号链路问题。
- **优化工具调用策略**：在处理此类逻辑状态类告警时，应优先调用 SOE（事件顺序记录）或“状态变位记录”工具，获取告警触发前后的时序数据，以判断是由于电网故障触发跳闸，还是信号突变导致的逻辑误判，从而避免仅依赖当前快照数据的片面性。
- **细化根因分类与操作指引**：将根因分类从宽泛的“软件”细化为“IO配置/接线/中间继电器”等具体层级；在操作建议中，具体增加“检查辅助触点接触不良”及“确认信号源供电（有源/无源）匹配”等针对信号链路的排查项，提升现场可操作性。
- **补充设备状态码定义库**：在提示词上下文中预置设备关键状态字（如 `OperatingStatus=2`）的明确定义表，确保 Agent 在第一步即可准确解析设备运行模式，减少因状态码含义模糊导致的推导步骤和分析误差。

---

## 2026-04-19T06:06:28.101+08:00 (alarmId: 8653)

- **增强领域知识的阈值判定标准**：在提示词中预置并网标准的具体阈值（如国标规定频率下限 49.8Hz、电压下限 198V），使 AI 能直接通过数值比对判定“越限”，无需在推理中反复确认标准引用，提升首轮响应准确度。
- **优化工具调用策略以降低迭代次数**：针对“离网”类告警，强制要求并行调用“PCS 保护状态查询”和“电表电能质量查询”工具，避免因串行查询导致的 6 次迭代消耗，提升分析效率。
- **引入源端数据交叉验证逻辑**：在将根因归咎于外部电网前，增加对比步骤：对比 PCS 自身采样读数与电表读数。若两者偏差大，优先排查 PCS 采样回路故障；若一致，再确认为电网侧问题，排除采样误差干扰。
- **预置典型故障模式特征**：补充“周期性瞬态扰动”模式库（特征：告警持续 1-2 秒、周期性触发、伴随电压/频率波动），以便 AI 能快速匹配此类“非设备故障”场景，减少不必要的软件故障排查路径。
- **增加故障后自恢复状态校验**：在最终结论中增加对“当前是否已恢复并网”的状态校验逻辑。若设备已自动恢复，应将操作建议调整为“仅记录监控”而非“立即核查”，以提高建议的精准度。

---

## 2026-04-19T06:07:21.843+08:00 (alarmId: 8659)

- 增加对“保护触发具体原因”的细粒度数据采集明确区分是电压越限还是频率越限导致离网，避免仅基于VF状态进行推测。
- 针对“频繁并离网循环”（如14次/26秒）场景，强化对“控制参数滞回（死区）设置”的逻辑检查，优先排查定值配置不当导致的振荡，而非直接归因于硬件误差。
- 补充具体的并网标准阈值知识（如GB/T 19964规定频率正常运行范围通常为49.5Hz-50.2Hz），以便在49.8Hz时能立即判断定值是否违反标准或配置过严。
- 预置“接触器/继电器动作反馈”检查项，在发生高频循环时，结合IO状态排查是否存在物理开关粘连或辅助触点抖动。
- 在分析工具调用序列中，优先引入“波形或高频采样数据”获取环节，仅凭平均数值无法判别是否为瞬时噪声或电压畸变导致的误动。

---

## 2026-04-19T06:07:49.752+08:00 (alarmId: 8663)

- **补全领域知识（状态 vs 故障）**：在知识库中明确界定“VF离网状态”为储能系统正常的运行模式（如孤岛支撑、黑启动），而非默认的软件故障。当Agent检测到此状态时，应优先预设为“模式切换”而非“设备故障”，以减少误判和推理回合。
- **优化工具调用顺序（Yx优于Yc）**：调整Agent的查询策略，优先调用状态类接口（如`getPcsYx`）确认工作模式（并网/离网），确认模式后再调用遥测类接口（`getPcsYc`、`getMeterYc`）。本次6次迭代显示早期可能进行了不必要的数据对比，优先锁定状态可提升效率。
- **预置“远程/就地”冲突典型故障模式**：增加针对控制权限冲突的预置规则。当出现“VF模式正常”但“Remote/Local位异常”时，应直接触发“通讯链路抖动”或“就地旋钮被误触”的排查路径，而非笼统的建议核对配置。
- **增强电压差异诊断逻辑**：针对PCS端口与电表侧电压偏差（215V vs 190V），逻辑中应增加“开关位置与拓扑影响”的判断分支。若检测到离网，应明确核查内部断路器状态或变压器档位，而非仅通过“参考系不同”做模糊解释。
- **结论表述去噪与聚焦**：在最终结论中，更清晰地区分“状态告知”与“故障告警”。对于本案例，应明确指出“VF离网本身无异常”，核心问题聚焦于“`pcsRemoteLocalStatus`异常”的处理，避免结论过于宽泛导致运维操作困惑。

---

## 2026-04-19T06:08:17.927+08:00 (alarmId: 8667)

- 预置“电压采样偏差”高置信度判别逻辑：当 PCS 与电表电压差异超过 30% 且电网频率/电压正常时，提示词应引导 Agent 优先将根因锁定为“CT/PT 变比设置错误”或“采样回路故障”，而非模糊地归因为“定值不匹配”，减少非必要推测。
- 优化工具调用策略以降低迭代次数：本次分析耗时 6 次，建议将设备遥测数据（PCS/电表）与保护定值配置项打包为一次性的复合查询任务，避免因缺乏定值数据而导致推理反复和验证缺失。
- 强化“配置数据”缺失时的推理约束：在未能获取 VF 保护定值的情况下，严禁将“定值不匹配”作为并列根因输出；应强制要求 Agent 将结论聚焦于已证实的“采样异常”，并将定值核查仅作为辅助排查建议。
- 建立“VF 离网误动”典型故障模式知识库：预置标准排查路径（电表正常 vs PCS 跳闸 -> 对比电压幅值 -> 校验采样变比 -> 检查接线），以规范 Agent 的推理路径，避免因数据点发散影响分析深度。
- 细化结论的概率权重与可操作性：建议在结论中对疑似根因进行量化（例如：90% 可能性为采样故障，10% 为定值问题），并针对 60V 的显著压差，直接给出“检查二次接线/量程设置”的具体步骤，而非笼统的“远程核查”。

---

## 2026-04-19T06:08:41.694+08:00 (alarmId: 8669)

- 完善PCS“VF离网”领域知识定义：明确储能场景下“VF离网状态”通常指设备进入离网（孤岛）运行模式以支撑本地负载，而非单纯的故障停机。需在提示词中区分“VF保护跳闸（停机）”与“VF模式运行（带载）”，避免将持续的功率输出（21.9kW）误判为保护失效。
- 优化推理逻辑中的状态冲突判别：当检测到`pcsVfOffGridStatus=true`（离网）但同时满足`pcsOverallFaultStatus=false`（无总故障）且有正向功率输出时，应优先判定为“电网故障触发主动孤岛切换”，逻辑上不应得出“保护失效导致未停机”的矛盾结论。
- 增设“电网扰动引发主动模式切换”典型故障模式：预置“电网电压不平衡/欠压触发PCS离网并转入孤岛供电”的标准分析链路，将该场景作为优先匹配项，减少对正常功能的误报。
- 强化关键否决数据的权重：在推理过程中，若`pcsOverallFaultStatus`为0且`pcsControlSoftwareFaultWord`均为0，必须触发逻辑分支排除“严重软件故障”或“定值配置错误”的假设，以实测运行状态为准。
- 纠正最终建议的导向：对于此类运行状态，操作建议应修正为“确认PCS是否处于孤岛保电策略，评估电网恢复条件”，而非建议排查“为何未停机”或“强制停机”，防止误导运维人员误切正在保电的设备。

---

## 2026-04-19T06:09:16.279+08:00 (alarmId: 8673)

- **修正告警分类与逻辑的冲突校验**：针对 VF 离网类告警，初始常被误标为“software”。建议在提示词中增加分类纠偏逻辑，若检测到电压/频率电气量越限，应强制排除纯软件故障假设，优先覆盖为“外部电网”或“保护动作”分类，减少误判干扰。
- **深化电压差异的定量分析能力**：本次分析中 PCS 端口电压（95.7V）远低于电表电压（174.4V），建议增加“电压降/阻抗分析”逻辑，通过对比并网点与 PCS 端口的压差，精确定位是电网侧整体偏低还是接触器/线路阻抗过大导致压降，提升结论颗粒度。
- **强化阈值调整建议的合规性约束**：在建议调整 VF 脱网阈值（如从 49.9Hz 调至 49.5Hz）时，应预置并网国家标准（如 GB/T 19964）知识库，强制 AI 检查建议值是否违规，明确标注“非持久性修改”或“需电网公司审批”，避免给出违规且高风险的操作建议。
- **优化工具调用策略以减少迭代**：针对此类告警，建议将电表数据查询、PCS 内部状态字查询及历史告警日志检索合并为“并行检索策略”，避免因分步排查导致 5 次迭代，将分析过程收敛在 1-2 轮以内。
- **预置“反复抖动”典型故障模式**：添加“短时间高频触发（如30秒内20次）”的模式识别，将其直接映射为“保护临界振荡”或“接触不良”特征，生成针对性排查项（如排查接线松动、二次侧采样干扰），缩短根因定位路径。

---

## 2026-04-28T17:15:58.879+08:00 (alarmId: 9088)

- **修正停机状态下的电压分析逻辑**：在 `pcsOperatingStatus=4`（停机）状态下，交流电压反映的是电网电压，直流电压反映的是母线电压，两者不存在运行时的调制比例关系。应剔除基于调制系数（0.8）的错误计算，转而分析采样偏差或保护定值触发逻辑。
- **强制引入时序数据（SOE）分析**：当前分析仅依赖实时快照，无法判断故障发生的先后顺序。建议在工具调用策略中强制加入 `getPcsSoe` 或历史曲线查询，以确认是电压异常导致停机，还是停机后的状态读数异常。
- **预置典型故障码的专家规则**：针对“软件故障-交直流电压不匹配”，应预置典型故障模式知识（如采样漂移、直流过压、电网异常），避免 AI 基于错误的物理模型（如停机态调制公式）进行推导，提高根因定位的准确性。
- **区分“故障现象”与“故障原因”状态位**：对于 `pcsRemoteLocalStatus`、`pcsGridTiedStatus` 等状态位，需结合逻辑判断其是导致故障的根因（如开关位置错误），还是故障发生后的被动结果（如保护动作导致的并网状态丢失），避免将伴随状态误判为原因。
- **细化采样校验的可操作性建议**：在操作建议中增加具体的量化校验步骤，例如“对比 PCS 直流电压与 BMS 总压偏差是否超过 2%”或“检查霍尔传感器供电”，而非仅建议“检查参数”，以提升现场排查效率。

---

## 2026-04-28T17:31:04.501+08:00 (alarmId: 9089)

- **增加迭代次数与Token预算的硬性熔断机制**：针对本次8次迭代导致Token超限的问题，设定最大迭代次数（如5次）或Token阈值（如150k），触发后强制Agent输出当前阶段性结论，而非直接报错。
- **预置“交直流电压不匹配”的标准诊断工作流**：针对此类Pcs常见故障，预置结构化排查步骤（如：检查采样值 -> 对比校准参数 -> 验证软件版本），避免Agent进行漫无目的的探索式搜索。
- **优化工具调用策略，减少非必要数据加载**：限制单次工具调用的返回数据量，优先使用带过滤条件的查询工具（如按时间窗口、关键词过滤），避免全量日志加载导致的Token快速消耗。
- **强化提示词中的“思维链压缩”指令**：要求Agent在每次迭代中仅保留关键推理步骤和中间结论，去除冗余的自我解释，以降低上下文长度。
- **改进降级策略，输出部分分析结果**：当资源耗尽时，不应仅提示“人工介入”，而应输出已完成的排查项（如“已排除硬件过压，建议人工检查软件逻辑”），为人工分析提供有效线索。

---