# ems-agent — AI 协作规范

> **版本** v1.0 ｜ **最后更新** 2026-05-02
> 仓库根 md 白名单:`README.md`、`CLAUDE.md`,其余入 `llm-ai/`。

## 项目元信息

| 项 | 值 |
|---|---|
| 技术栈 / 包管理器 | Node.js + TypeScript (ESM) / npm |
| 接口基地址(开发/生产) | EMS_BASE_URL 环境变量配置 / 同左 |
| 鉴权 / HTTPS 状态 | 无（内网直连）/ 未声明 |
| Mock 状态 | 无 Mock，直连 EMS Java 后端 |

其他信息(Node 版本 / 浏览器目标 / 部署目标 / 分支策略 / 代码规范来源)见 `README.md`,不在此重复。

## 五条铁律

### 铁律 1:分级自评审

按任务级别(L1-L5,见下表)输出对应维度的交付。**不对齐级别 = 违规**(小任务配大模板,或大任务糊小模板,都不可)。

| 级别 | 判据(影响半径) | 方案预批 | 自评审维度 |
|---|---|---|---|
| **L1** | 样式/布局/文案,仅视觉层 | 否 | 0 — 1 句话改动清单即可 |
| **L2** | 单文件内部逻辑,不改签名不改接口 | 否 | 2 — 边界 / 回滚 |
| **L3** | 跨 2+ 文件 / 改共享状态 | **是** | 5 — 改动清单 / 波及 / 边界 / 回滚 / 回归 |
| **L4** | 改公共签名 / 接口契约 / 数据模型 | **是** | 8 全维度 + 调用方清单 |
| **L5** | 新增模块 / 迁移 / 架构重构 | **RFC 文档** | 全量 + 用户批准点 |

8 维度自评审(L4/L5 用):**改动清单 / 波及调用方 / 边界 / 异常 / 回滚 / 回归点 / 污染面 / 安全边界**(鉴权 / RBAC / 校验 / XSS 入口如 `v-html` `dangerouslySetInnerHTML` / 日志禁打印 token 密码)。

### 铁律 2:踩坑登记(去重)

- **新踩坑**(项目里第一次遇到这类问题)→ `error-<scope>-<topic>-YYYY-MM-DD.md`,scope ∈ `{security,deps,api,ui,perf,flow}`;首次创建时同时生成 `llm-ai/踩坑记录索引.md`(若未存在),添加 grep 关键词映射
- **同一坑的迭代修复**(第 2/3/N 次改)→ **追加到原 md 的"迭代记录"段,不新建**
- 修复完成 → `fix-*` 对应 md;commit `<type>(<scope>): <中文>`,type ∈ `{feat,fix,refactor,docs,chore,style,test}`

**判别边界**:同一文件同一模块的连续调整 = 同一坑;跨模块跨主题 = 新坑。

### 铁律 3:影响半径判级

写方案前先判 L1-L5,按铁律 1 对应交付粒度。判级凭据文件:行号。判级结果在回复开头一句话亮明(例:`[L2 · 单组件内部调整]`)。

### 铁律 4:根目录洁癖

md 必入 `llm-ai/`,根目录白名单仅 `README.md`、`CLAUDE.md`。违反 `git mv` + 补登。

### 铁律 5:索引轻量闭环

按改动面更新 — 新增删文件→§1§2;改数据流→§3§5;新约定/坑→§6;未决→§7;**§8 允许一句话,trivial 改动可省**。索引 commit 用 `docs(index): ...`。

## 代码行为规约(4 条)

> 这 4 条管"写代码的心法",与下一节"AI 执行规范"(token 节流)并列。
> **tradeoff**:偏向谨慎而非速度。trivial 任务可用判断力适度放宽。

### 1. 先思后码

**不臆测、不藏糊涂、暴露 tradeoff**。

- 假设要**明说**,不确定就问
- 多种解释并存 → **全部呈现**,不擅自挑一个
- 有更简单的方案 → **说出来**,必要时反推
- 卡壳点 → 停、指出、问(不硬装懂)

与 AI 执行规范 5 的关系:那是**操作层**(不确定给 2-3 选项),本条是**意识层**(先觉察到不确定)。

### 2. 简洁优先

**用最少代码解决问题,不加没要求的东西**。

- 不加没要求的功能
- 不为一次性代码做抽象
- 不加没要求的"灵活性 / 可配置性"
- 不处理不可能发生的错误
- 写了 200 行但 50 行能搞定 → **重写**

自检:"资深工程师会不会说这过度设计了?" 会 → 简化。

### 3. 外科手术式改动

**只碰必须碰的,只清自己制造的垃圾**。

改现有代码时:
- 不"顺手"改相邻代码、注释、格式
- 不重构没坏的东西
- 匹配现有风格,即使你觉得有更好的写法
- 发现不相关的死代码 → **提一下,别删**

你的改动产生孤儿时:
- 清掉**你的改动产生的**无用 import / 变量 / 函数
- 不删预先存在的死代码(除非用户明确要求)

**检验**:每一行改动都要能直接追溯到用户请求。

与铁律 1(L1-L5 分级)配合:L1 任务本条效力最强(只改视觉),L4/L5 任务本条让位于"安全边界 / 调用方清单"等更重维度。

### 4. 目标驱动执行

**定义成功标准,闭环到验证通过**。

把任务转成可验证目标:
- "加校验" → "写非法输入测试,跑过"
- "修 bug" → "写复现测试,跑过"
- "重构 X" → "前后测试都跑过"

多步任务列简要计划:

```
1. [步骤] → 验证:[检查]
2. [步骤] → 验证:[检查]
3. [步骤] → 验证:[检查]
```

强成功标准 = AI 可独立闭环;弱标准("让它跑起来")= 反复来回问。

与铁律 1 的关系:L1-L2 用本条的"简要计划"即可;L3+ 升级到铁律 1 的"方案预批 + 自评审维度"。

**这 4 条生效的信号**:diff 里无关改动变少、因过度设计重写变少、澄清问题出现在实现前而非出错后。

## AI 执行规范(token 节流)

1. **同类覆盖一次写宽**:CSS/伪类/状态问题,第一轮就覆盖所有层 × 所有伪类 × 所有状态,宁可过度也别留缝。宽选择器的代价远低于再来一轮。
2. **同文件 ≥ 2 次 edit → 一次 Write**:连续小修不如整体重写。
3. **已在上下文的文件不重复读**:grep/Read 前先判断必要性;多个文件说同一件事,只读权威档(原档 > 索引 > 代码)。
4. **默认去仪式化**:回复不带"要不要 push"/"是否需要"这类尾巴,除非有真实歧义需用户决策。
5. **不确定给选项**:拿捏不准的方向提供 2-3 个候选让用户选,不自问自答也不赌一边。

## 按需路由表(替代"开工前必读")

遇到问题按主题查对应文件,命中才读:

| 问题主题 | 读 |
|---|---|
| 踩过类似坑吗 | `llm-ai/踩坑记录索引.md` grep 关键词 → 命中读具体 `error-*.md` |
| 代码怎么写符合风格 | `llm-ai/代码样式.md` 对应维度 |
| 目录在哪 / 模块边界 | `llm-ai/代码索引.md §1-§3` |
| 找函数定义 | **grep,不查索引** |
| 体检历史中等项 | `llm-ai/体检记录.md` |
| 边界情况怎么处理 | `llm-ai/边界处方.md` |
| 领域知识 / 推理规则 | `KNOWLEDGE.md`(热加载) |
| 历史经验教训 | `self-improvement.md`(热加载) |

**字段冲突优先级**:KNOWLEDGE.md > 代码实现 > 推测(最低)。冲突先问用户,不擅自按代码修正文档。

## 文档过时检测

**发现脱节就改,不设定期节律**。Session 中如果发现:§1/§2 与 src/ 不一致、grep 关键词映射失效 → 当下修。不能判断的入 §7。同步更新文档顶部时间。

## 目录约定

```
ems-agent/
├── CLAUDE.md / README.md
├── KNOWLEDGE.md           (领域知识库,热加载)
├── self-improvement.md    (AI自我改进积累,热加载)
└── llm-ai/
    ├── 代码索引.md
    ├── 踩坑记录索引.md        (第一次遇坑时才生成)
    ├── 代码样式.md
    ├── 体检记录.md           (第一次出现中等项才生成)
    ├── 边界处方.md
    └── error|fix-<scope>-<topic>-YYYY-MM-DD.md
```

## 违反处理

1. 立即停手
2. 撤销:未推 `git reset`;已推 `git revert`(**禁 `push --force`**);位置错 `git mv` 入 `llm-ai/`
3. 补 `error-flow-<topic>-YYYY-MM-DD.md`
