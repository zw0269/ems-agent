# EMS Agent 知识沉淀文档

## 告警优先级语义（P3 最严重）

| 优先级 | 含义 | 对应 EMS level | 处理策略 |
|---|---|---|---|
| **P3** | 紧急 / 一级，可能设备损坏或安全事故 | level="3" | 快速通道（200ms 轮询）+ 邮件 + 钉钉 |
| **P2** | 重要 / 二级，影响系统运行 | level="2" | 主队列 + 邮件 + 钉钉 |
| **P1** | 一般 / 三级，需关注 | level="1" | 主队列 + 邮件 + 钉钉 |
| **P0** | 提示 / 四级，仅日志 | level="0" | 跳过 LLM，直接入库记录 |

> 注意：本工程中 **P3 严重度最高**，与部分业界"P0=最高"的约定相反。代码里所有排序权重、路由、通知策略均以此为准。

## 核心点位说明 (CORE_FIELDS)

| 点位 Key | 含义 | 单位 | 备注 |
|---|---|---|---|
| bms_total_voltage | 总电压 | V | 监控整簇电压，判断充电上限 |
| bms_total_current | 总电流 | A | 正值为充电，负值为放电 |
| bms_soc | 荷电状态 | % | 剩余容量比例 |
| bms_max_cell_voltage | 最高单体电压 | V | 保护单体不被过充，阈值 3.65V |
| bms_min_cell_voltage | 最低单体电压 | V | 保护单体不被过放，阈值 2.8V |
| pcs_charge_limit | 充电功率限制 | kW | PCS 侧设置的充电上限 |

## 额外点位逻辑 (EXTRA_FIELDS)

- **cell_voltage_high**: 需要额外查看 `bms_cell_voltages`（所有单体电压）和 `pcs_charge_limit`，判断是否为个别电芯失控或 PCS 整体过充。
- **cell_temp_high**: 需要联动 `cooling_system_status`（冷却系统状态），判断是产热过大还是散热失效。

## 通用推理规则（R1–R5，从历史案例沉淀）

这五条规则是所有软件/配置类告警分析的**强制前置检查**，先跑这五步再进入具体故障树。

### R1 数据一致性前置校验（三项自洽检查）

每次推理第一步，必须跑下列三项：

1. **状态 vs 功率/电流方向**：若 `operatingStatus="备用"/"停机"` 但 `|current| > 10A` 或 `|power| > 5kW`，优先判定为 **信号链/状态采样故障**，不再深挖硬件。
2. **分压和 vs 总压**：若 `UpVol + DownVol` 与 `TotalDirectVol` 偏差 > 5%（如 114V 级差），优先判定为 **采样系数配置/寄存器映射/通讯解析错误**，不是物理电压不匹配。
3. **PCS 采样 vs 电表采样**：若同一点位的两路采样偏差 > 10%，先校验 **CT/PT 变比、二次接线、屏蔽接地**，再谈电网问题。

任一项不自洽 → 直接收敛到信号链/采样故障分支，禁止继续沿"硬件损坏"方向扩散。

### R2 定值未知时不下绝对结论

在未读取到具体保护定值（如 `pcs_ov_threshold`、`F_low_set`、回差 `dead_band`、延时 `time_delay`）前：

- "阈值配置不匹配"只能作为**≤50% 疑似项**输出，必须与"真实越限"并列保留。
- 操作建议里必须包含"读取 PCS 内部保护寄存器"这一具体步骤，而非笼统的"核查定值"。
- 禁止直接建议"放宽阈值"或"调整至 49.5Hz"等具体修改，除非已有并网标准（GB/T 19964）背书 **且** 现场确认为频繁性越限。

### R3 振荡/抖动模式优先识别

检测到以下特征之一：

- 30 秒内 ≥ 2 次触发并自动恢复
- 14 次/26 秒级别的并离网循环
- 20 次/30 秒级别的高频反复

→ 直接跳转到以下排查分支，**禁止进入硬件故障树**：

1. 保护定值死区（回差）过小
2. 二次侧采样干扰（屏蔽层、接地）
3. 接触器/继电器辅助触点抖动、粘连
4. 通讯抖动导致的状态字误置

### R4 VF 状态三分类

出现 `pcsVfOffGridStatus=true` 或 "VF 离网" 类告警时，**必须先分类再推理**：

| 子类 | 特征 | 处理 |
|---|---|---|
| ① VF 保护跳闸（停机） | `operatingStatus=0` 且无功率输出 | 按保护动作分析，检查保护定值与越限数据 |
| ② VF 模式孤岛带载 | `operatingStatus=运行` 且有功率输出（>5kW） | 正常工作模式，**禁止建议"强制停机"**；操作建议应为"确认保电策略、评估电网恢复条件" |
| ③ VF 标志位误置 | 总故障=0、软件故障字全 0、功率/状态无异常 | 优先查状态采样逻辑、IO 配置、接触器反馈 |

分类错误会导致"VF 正常孤岛运行被误判为故障"的严重误导。

### R5 工具调用批量化（首轮全景上下文）

为把目前普遍 5–8 次的迭代压到 ≤ 2 次，**首轮必须并行调用**以下一组数据：

- `getPcsYx`（状态/故障/告警遥信）
- `getPcsYc`（实时遥测）
- `getMeterYc`（并网点电表数据，用于 R1 第 3 项对比）
- 保护定值寄存器读取（供 R2 使用）
- 告警前后 ±60s 的历史趋势 / SOE 事件顺序记录（供 R3 使用）

在这一包数据完整前不得输出初步结论，避免因数据碎片导致反复查询。

## 典型故障模式决策树（工程级识别规则）

### 模式 1："运行态告警"（设备正常但有告警）

**识别特征**：
- `pcsOperatingStatus` 正常（运行/并网）
- `pcsOverallFaultStatus = false`（总故障状态正常）
- 存在单点告警（如某个保护触发）

**推理路径**：
1. **优先假设**：定值配置不合理 / 采样回路偏差 / 信号误发
2. **禁止假设**：真实硬件故障（因设备仍在正常运行）
3. **排查顺序**：
   - 检查保护定值是否过于敏感（对比行业标准）
   - 检查采样回路是否存在偏差（R1.3 交叉验证）
   - 检查信号链路是否存在干扰/误触发

**案例参考**：TEST-1774925781037（243V触发过压告警但设备运行正常）

---

### 模式 2："VF 离网振荡"（高频反复触发）

**识别特征**：
- 30 秒内 ≥ 2 次触发并自动恢复
- 或 14 次/26 秒、20 次/30 秒级别的高频循环

**推理路径**：
1. **直接跳转**：保护定值死区过小 / 采样干扰 / 接触器抖动
2. **禁止进入**：硬件故障树（因为能自动恢复说明硬件正常）
3. **排查顺序**：
   - 检查保护定值回差（dead_band）是否过小
   - 检查二次侧采样是否存在干扰（屏蔽层、接地）
   - 检查接触器/继电器辅助触点是否抖动、粘连
   - 检查通讯链路是否存在抖动导致状态字误置

**案例参考**：8659（14次/26秒并离网循环）、8673（20次/30秒高频反复）

---

### 模式 3："状态矛盾"（总故障正常但有P1告警）

**识别特征**：
- `pcsOverallFaultStatus = false`（总故障状态正常）
- 但存在 P1 级别的子系统故障信号（如 sort=1）

**推理路径**：
1. **优先假设**：配置逻辑错误 / 状态位定义不一致
2. **禁止假设**：真实电网故障（因总故障状态正常）
3. **排查顺序**：
   - 检查总故障状态与子系统故障的逻辑一致性
   - 检查状态位定义是否存在歧义
   - 检查是否为状态机卡死或中间过渡态

**案例参考**：8635（总故障正常但存在P1故障信号）

---

### 模式 4："电压不匹配"（PCS与电表电压偏差大）

**识别特征**：
- PCS 采样电压与电表采样电压偏差 > 10%
- 或母线电压内部不自洽（UpVol + DownVol ≠ TotalDirectVol）

**推理路径**（三级排查）：
1. **采样故障**（数据自相矛盾）：
   - 母线电压算术不符 → 采样系数配置/寄存器映射/通讯解析错误
   - PCS与电表偏差大 → CT/PT变比错误/二次接线松动/屏蔽接地不良
2. **参数标定错误**（数据稳定但存在固定偏差）：
   - 检查采样系数、量程设置
   - 检查变比配置是否与实际硬件匹配
3. **硬件预充电失败**（电压真实无法建立）：
   - 仅在排除前两项后才考虑
   - 检查预充电回路、接触器、母线电容

**案例参考**：8352（母线电压差114V）、8667（PCS与电表电压差60V）

---

### 模式 5："VF 离网状态"（需三分类处理）

**识别特征**：
- `pcsVfOffGridStatus = true` 或收到 "VF 离网" 类告警

**三分类判断**：

| 子类 | 特征 | 处理 | 禁止操作 |
|---|---|---|---|
| ① VF 保护跳闸（停机） | `operatingStatus=0` 且无功率输出 | 按保护动作分析，检查保护定值与越限数据 | - |
| ② VF 模式孤岛带载 | `operatingStatus=运行` 且有功率输出（>5kW） | 确认保电策略、评估电网恢复条件 | **禁止建议"强制停机"** |
| ③ VF 标志位误置 | 总故障=0、软件故障字全0、功率/状态无异常 | 优先查状态采样逻辑、IO配置、接触器反馈 | - |

**案例参考**：8669（VF离网但有21.9kW输出，误判为故障）、8663（VF状态正常但被误报）

---

### 模式 6："停机态电压异常"（停机时的电压分析）

**识别特征**：
- `pcsOperatingStatus = 4`（停机）或 `= 0`（停机）
- 报告交直流电压不匹配

**物理模型修正**：
- **停机时**：交流电压 = 电网电压，直流电压 = 母线电压
- **不存在**：调制比例关系（调制系数仅在运行时有效）
- **禁止**：基于调制系数（如0.8）计算期望电压

**推理路径**：
1. 分析采样偏差或保护定值触发逻辑
2. 引入时序数据（SOE）确认故障发生先后顺序
3. 区分"电压异常导致停机"还是"停机后的状态读数异常"

**案例参考**：9088（停机态误用调制系数计算）

---

## 保护定值标准库（GB/T 19964 并网标准）

### 交流电压保护

| 保护类型 | 正常范围 | 预警上限 | 保护触发 | 备注 |
|---|---|---|---|---|
| 相电压过压 | 220V ±7% | 242V | 260-270V | 242V为预警，260V以上才是真过压 |
| 相电压欠压 | 220V ±7% | 198V | 185-190V | 低于185V触发保护 |
| 电压不平衡度 | - | 1% | >2% | 负序分量百分比 |

### 频率保护

| 保护类型 | 正常范围 | 保护触发 | 回差建议 | 备注 |
|---|---|---|---|---|
| 频率上限 | 49.5-50.5Hz | 50.2-50.5Hz | ≥0.1Hz | GB/T 19964标准 |
| 频率下限 | 49.5-50.5Hz | 49.5-49.8Hz | ≥0.1Hz | 低于49.5Hz触发保护 |

### 直流电压保护（1000V系统）

| 保护类型 | 正常范围 | 保护触发 | 备注 |
|---|---|---|---|
| 直流过压 | 700-1000V | 950-1000V | 根据BMS额定电压调整 |
| 直流欠压 | 700-1000V | 650-700V | 低于650V可能无法正常运行 |

### 合规性约束

- **调整保护定值前必须**：
  1. 确认是否违反 GB/T 19964 标准
  2. 标注"需电网公司审批"（如涉及并网点保护）
  3. 确认为频繁性越限（非偶发）
- **禁止建议**：
  - 放宽至标准范围外的阈值
  - 在单次告警后立即调整定值
  - 在缺乏录波证据时调整定值

## LLM 提示词迭代记录

- **v1.0 (Initial)**: 分离硬件和软件分析。硬件走单次报告，软件走 Agent Loop 循环推理。
- **v1.1 (2026-04-22)**: 引入 R1–R5 规则；告警优先级语义翻转（P3 最严重）；self-improvement.md 由时间序列堆积改为高频缺陷精炼版。

## 故障处理 SOP

1. **P3 告警**: 触发钉钉 + 邮件双重通知，进入 P3 快速通道（200ms 轮询），运维人员需在 15 分钟内响应。
2. **P2 告警**: 触发钉钉 + 邮件，运维人员需在 1 小时内响应。
3. **P1 告警**: 触发邮件通知，运维人员需在 4 小时内处理。
4. **P0 告警**: 跳过 LLM，直接入库记录，仅作日志/日报参考。
